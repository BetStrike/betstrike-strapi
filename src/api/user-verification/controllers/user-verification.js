'use strict';

/**
 * user-verification controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::user-verification.user-verification');
